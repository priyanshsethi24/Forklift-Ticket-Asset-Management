export const baseURlNoAPI = "http://103.206.101.254:8800/"
export const baseURL = `${baseURlNoAPI}/api`;
// // export const baseURL = "https://1b21-125-63-73-50.ngrok-free.app/api";

// const environment = process.env.ENVIRONMENT;

// let baseURlNoAPI;
// if (environment === 'test') {
//     baseURlNoAPI = process.env.REACT_APP_TEST_URL;
// } else if (environment === 'stage') {
//     baseURlNoAPI = process.env.REACT_APP_STAGE_URL;
// } else if (environment === 'production') {
//     baseURlNoAPI = process.env.REACT_APP_PRODUCTION_URL;
// } else {
//     baseURlNoAPI = "http://127.0.0.1:8000"; 
// }

// export const baseURL = `${baseURlNoAPI}/api`;

// base url without '/api'

export const baseNew =  `${baseURlNoAPI}`;
 